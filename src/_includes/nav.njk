<nav aria-label="Main Navigation">
  <ul class="nav">
    <li {% if page.url == "/" %}class="current-page"{% endif %}><a href="/">Home</a></li>
    {% for home in collections.homes %}
      <li {% if page.url == home.url %}class="current-page"{% endif %}>
        <a href="{{ home.url }}">
          {{ home.data.title }}
        </a>
      </li>
  {% endfor %}
  </ul>
</nav>
